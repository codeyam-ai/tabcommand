import React from 'react';

// Stub: the search box + SearchResults are built in the `search` plan. Rendered
// as an inert sidebar placeholder so the Home layout is whole without pulling
// search behavior forward.
const Search = () => (
  <div className="Search">
    {/* stub: built in search */}
  </div>
);

export default Search;
