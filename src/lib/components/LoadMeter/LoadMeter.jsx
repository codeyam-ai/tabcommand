import React from 'react';

// Stub: the load gauge widget + Load page are built in the `load-meter` plan.
// Rendered as an inert sidebar placeholder; App wires its onClick to navigate to
// the LOAD page (a coming-soon placeholder until that plan).
const LoadMeter = () => (
  <div className="LoadMeter">
    {/* stub: built in load-meter */}
  </div>
);

export default LoadMeter;
