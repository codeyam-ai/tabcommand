import React from 'react';

// Stub: Labels / LabelCollection / LabelForm and the real drag-to-group +
// reorder behavior are built in the `labels-and-dnd` plan. Rendered as an empty
// container so the Home DragDropContext has its second child and the layout is
// whole.
const Labels = () => (
  <div className="Labels" id="Labels">
    {/* stub: built in labels-and-dnd */}
  </div>
);

export default Labels;
