> auto-backgrounded — the harness completion notification arrives on its own when this exits; you do NOT need to do anything to receive it, and waiting adds nothing. KEEP WORKING: continue with anything that does not depend on this result — the notification interrupts you when it lands. "Do not poll" does NOT mean "stop working": ending the turn to wait is the one response that is never correct. ONLY if you need the result inside the CURRENT turn, block with: codeyam-editor editor wait-for /tmp/claude-0/-workspace/bbdfa599-346b-4dbe-91ce-32de7f328abc/tasks/b6517c6ge.output (ONE call, bare — never piped through `| tail`; its budget is capped to fit the tool-call ceiling, so a long run returns WAIT_FOR STILL_RUNNING in-call and the notification still arrives. Re-issue only on LIKELY_WEDGED; do NOT hand-roll an `until grep … sleep` loop, the harness blocks it). Just want to know it is still alive? Read /tmp/claude-0/-workspace/bbdfa599-346b-4dbe-91ce-32de7f328abc/tasks/b6517c6ge.heartbeat — one line, always the newest heartbeat; the output file below stays clean of them. Output too long? You do not need `| tail`: run it bare and read the `CODEYAM_FULL_OUTPUT` line the command prints — it names a file holding the COMPLETE output, which you can `cat` afterwards.
{"command":"refresh-tests","elapsed_secs":0,"emitted_at_unix":1786621481,"heartbeat":"CODEYAM_CMD_RUNNING","note":"still running","pid":1073045}

note: --test name(s) not yet in the registry (copies the Current snapshot when its own Copy button is clicked) — scoping the run to the current diff so they run without registration first:
  src/lib/components/ExportPanel/ExportPanel.jsx
  src/lib/pages/ImportExport/ImportExport.test.jsx
note: the named tests run in Targeted mode, narrowed to the changed package(s). No --runner or raw-runner fallback is needed; register them at the Reconcile step as usual.
Running tests via the configured runner command(s) — use these, do not reconstruct runner flags:
  tests  ::  npx vitest run --reporter=json
Test refresh triggered. Waiting for results...
[   2s] Running runner: tests
[  12s] Still running: tests
[  22s] Still running: tests
[  32s] Still running: tests
[  38s] Tests completed.
{"failed":0,"passed":1,"skipped":958,"success":true,"total":1}
All 1 tests passed.

Partial run — partition caches WERE updated; the full-suite test-cache.json was
NOT (full-suite hash invariant). If a stale-partition bail sent you here, that
recovery has landed — re-run the command that bailed.
Audit gates read the full-suite cache. Flag-free `refresh-tests` updates it but
is gated until `finalize`. To update the full-suite cache now (e.g. for
brand-new tests), run: codeyam-editor editor refresh-tests --force
